@using Sandbox;
@using Sandbox.UI;
@inherits Sandbox.UI.NavHostPanel
@namespace Scenebox.UI
@attribute [StyleSheet]

<root>
    <div class="navigator-canvas" slot="navigator-canvas"></div>
</root>

@code
{
    public MainMenuPanel()
    {
        DefaultUrl = "/";

        AddDestination("/", typeof(MainMenuFrontPage));
        AddDestination("/start", typeof(StartGamePage));
        AddDestination("/find", typeof(FindGamePage));
    }
}